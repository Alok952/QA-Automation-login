const locators = {
  LOGIN_PAGE: {
    USERNAME_FIELD: "user-name",  // ID should be a simple string without the # symbol for `By.id`
    PASSWORD_FIELD: "password",
    LOGIN_BUTTON: "login-button",
    ERROR_MESSAGE: "error-message-container"
  }
};
export default locators;
